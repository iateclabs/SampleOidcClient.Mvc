@{
    ViewBag.Title = "RestrictedContent";
}

<h2>RestrictedContent</h2>
<p>@ViewBag.Message</p>
<h3>Claims</h3>
<ul>
@foreach (var claim in (User as System.Security.Claims.ClaimsPrincipal)?.Claims) {
	<li><b>@claim.Type:</b> @claim.Value</li>
}
</ul>

